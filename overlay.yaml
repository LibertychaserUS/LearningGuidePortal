# Local Overlay config. Pin is this repo SHA, not main/HEAD.
# Adopter thin file only — do not vendor overlay/.
# overlay-check checks out overlay-v1.0.0 in the runner and runs suite
# product_command when those files exist. That is not Overlay-armed.
schema: overlay-config/v1
product:
  repo: LibertychaserUS/LearningGuidePortal
  default_ref: d06d15abaaefd001141dbe6a739362f2aefca3b4
branches:
  main:
    run: [functional, regression]
kinds:
  concurrency: later
  agent: later
never_red_statuses:
  - draft
  - blocked
forbid_hosts:
  - ilovelearningguide.com
