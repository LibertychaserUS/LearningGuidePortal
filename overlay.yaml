# Local Overlay config. Pin is this repo SHA, not main/HEAD.
# Adopter thin file only — do not vendor overlay/.
schema: overlay-config/v1
product:
  repo: LibertychaserUS/LearningGuidePortal
  default_ref: 6d8934e5811e371554a94aa47b2f56c40bf0cbd3
branches:
  main:
    run: [functional, regression]
kinds:
  concurrency: later
  agent: later
never_red_statuses:
  - draft
  - blocked
forbid_hosts:
  - ilovelearningguide.com
