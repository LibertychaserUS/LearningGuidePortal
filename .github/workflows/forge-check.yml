# Forge 门（design.md §3.5 的 guard）：checkout 发布针 forge-v1.1.2 → python -m forge check。
# 对 PR：deny_paths（工作流不许从 PR 里改）、suite_guard（agent 分支不许把套件改成 blocked）、
# docs_sync（改了 suites/inbox/配置要同 PR 改 brief；链接与版本引用有效）、pr-title、overlay validate/cover。
# 另校验 docs/STATE.md 是否与 forge.yaml / 工作流一致。不 live-apply，不 merge，不推。
# 此文件在 forge.yaml deny_paths 里。

name: forge-check

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  forge-check:
    name: forge-check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout product
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Checkout Forge forge-v1.1.2
        uses: actions/checkout@v4
        with:
          repository: LibertychaserUS/AIOps
          ref: forge-v1.1.2
          path: _aiops
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Forge
        run: |
          pip install -r _aiops/requirements.txt
          echo "PYTHONPATH=${GITHUB_WORKSPACE}/_aiops" >> "$GITHUB_ENV"

      # pull_request 才有标题。push 上 github.event.pull_request.title 是空串，
      # 若仍写入 PR_TITLE，forge-v1.1.2 会当成「有标题」并判 empty PR title。
      # 省略 PR_TITLE 时 pr-title 跳过（设计如此）；deny_paths / docs_sync 照跑。
      - name: forge check
        if: github.event_name == 'pull_request'
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: python -m forge check --root .

      - name: forge check
        if: github.event_name != 'pull_request'
        run: python -m forge check --root .

      - name: STATE is fresh
        run: python -m forge status --root . --repo ${{ github.repository }} --check-state
